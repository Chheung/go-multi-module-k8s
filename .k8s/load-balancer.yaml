apiVersion: v1
kind: Service
metadata:
  name: go-k8s-lb
spec:
  type: LoadBalancer
  selector:
    app: go-k8s-ingress
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3001
    - name: https
      protocol: TCP
      port: 443
      targetPort: 3001
